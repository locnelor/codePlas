import { SysSoftwarePlatformEntity } from './sys.software.platform.entity';

describe('SysSoftwarePlatformEntity', () => {
  it('should be defined', () => {
    expect(new SysSoftwarePlatformEntity()).toBeDefined();
  });
});
