import { SysTestPaperOnQuestionEntity } from './sys.test.paper.on.question.entity';

describe('SysTestPaperOnQuestionEntity', () => {
  it('should be defined', () => {
    expect(new SysTestPaperOnQuestionEntity()).toBeDefined();
  });
});
