import { SysTestSourceEntity } from './sys.test.source.entity';

describe('SysTestSourceEntity', () => {
  it('should be defined', () => {
    expect(new SysTestSourceEntity()).toBeDefined();
  });
});
